hl.config({
    dwindle = {
        preserve_split = true,

        smart_split = false,

        force_split = 0,

        smart_resizing = true,
    },
})

hl.config({
    misc = {
        disable_autoreload = false,
    },
    binds = {
        scroll_event_delay = 0,

        window_direction_monitor_fallback = false,
    },
})
